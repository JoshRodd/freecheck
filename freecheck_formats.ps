% freecheck_formats.ps
%
% This is a list of variables that will be filled in with data from the
% ~/.freecheck.cfg file (or from command-line parameters that overrode it).
% If a variable isn't specified, it will be simply be left blank here.
% Those variables will then be available in PostScript.
%
% An entry of /value means that a reference to another variable (such as
% a font name) will be encoded.
%
% An entry of {value} means a number will be encoded; if a size, the default
% will be a size in points, but could also be a size in inches.
%
% An entry of (value) means a string will be encoded, including escaping
% necessary. The other two types do not do any escaping.
%
% Anything else will be passed through unchanged.

% Globals
MICRFontName		/value
MICRFontSize		{value}
TransitSymbol		(value)
OnUsSymbol		(value)
AmountSymbol		(value)
DashSymbol		(value)
MICRVerTweak		{value}
MICRHorTweak		{value}
% Account
CheckNumber		{value}
PrintCheckBody		{value}
PrintMICRLine		{value}
NumPages		{value}
Name1			(value)
Name2			(value)
Address1		(value)
Address2		(value)
CityStateZip		(value)
PhoneNumber		(value)
BankName		(value)
BankAddr1		(value)
BankAddr2		(value)
BankCityStateZip	(value)
AuxOnUs			(value)
Routing			(value)
OnUs			(value)
Fraction		(value)
PrintVOID		{value}
% Styles
CheckLayout		/value
StandardFontName	/value
StandardFontSize	{value}
BoldFontName		/value
BoldFontSize		{value}
CheckNumDigits		{value}
CheckNumFontName	/value
CheckNumFontSize	{value}
FractionFontName	/value
FractionFontSize	{value}
Underline		{value}
LineWidth		{value}
MemoLineHeight		{value}
SignatureLineHeight	{value}
BankInfoHeight		{value}
AmountLineHeight	{value}
PayeeLineHeight		{value}
DateLineHeight		{value}
LogoFile		(value)
LogoWidth		{value}
LogoHeight		{value}
LogoPadding		{value}
LogoBorder		{value}
LogoBBox		[value]
BankLogoFile		(value)
BankLogoWidth		{value}
BankLogoHeight		{value}
BankLogoPadding		{value}
BankLogoBorder		{value}
BankLogoBBox		[value]
% Check Blank Types
CheckHeight		{value}
CheckWidth		{value}
CheckHorOffset		{value}
CheckVerOffset		{value}
ChecksPerPage		{value}
LeftMargin		{value}
RightMargin		{value}
TopMargin		{value}
